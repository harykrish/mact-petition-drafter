# Copy to .env (gitignored) or set in your Render/Railway dashboard.
# Get a key + the Build Day $500 credits from the offer link.
ANTHROPIC_API_KEY=sk-ant-...

# Model is pinned in code to claude-opus-4-8; override only if you must.
# MACT_MODEL=claude-opus-4-8
